<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="userMapper">
    <insert id="insert" parameterType="domain.User">
        INSERT INTO user (id, pwd, email, phone)
        VALUES (#{id}, #{pwd}, #{email}, #{phone})
    </insert>
    <select id="getUser" parameterType="domain.User" resultType="domain.User">
        SELECT * FROM user
        WHERE id = #{id} AND pwd = #{pwd}
    </select>
    <update id="updateLastLogin" parameterType="domain.User" >
        UPDATE user
        SET lastlogin = now()
        WHERE id = #{id}
    </update>
    <update id="update" parameterType="domain.User">
        UPDATE user
        SET pwd = #{pwd},
            email = #{email},
            phone = #{phone}
        WHERE id = #{id}
    </update>
    <delete id = "delete" parameterType="domain.User">
        DELETE FROM user
        WHERE id = #{id}
    </delete>

</mapper>